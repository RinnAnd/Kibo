import { extendTheme } from "@chakra-ui/react";

export const theme = extendTheme({
    colors: {
        primary: "#7C01F6",
        primaryComp: "#A142FF",
        secondary: "#EDDBFF",
        darkColor: "#101010",
        lightColor: "#C8C8C8",
        window: "#1F1F1F",
        input: "#373737",
    },
    fonts: {
        global: "Lilita One, cursive"
    }
})